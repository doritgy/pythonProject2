#You can't use an index for a set item because the "set" object'
#has a "hash" index, an algorithm that works very fast,
#so the set uses the hash algorithm and decides where the item will appear,
#every time in a different place